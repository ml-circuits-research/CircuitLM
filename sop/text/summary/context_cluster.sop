@input item
@input all
@input level default 0
@needs kernel.value.get value $item key "needsContext"
@args kernel.value.record item $item all $all level $level
@items kernel.flow.choose condition $needs then "text.summary.context_previous" else "text.summary.singleton" with $args
@output result $items
